<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- General -->
    <style name="Base.Theme.Aprobar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/blue</item>
        <item name="android:windowLightStatusBar">false</item><!-- false = iconos claros -->
    </style>

    <style name="Theme.Aprobar" parent="Base.Theme.Aprobar" />

    <!-- Menu -->
    <style name="navigation" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">20sp</item>
    </style>
</resources>